[package]
name = "test_n0_dasl"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
ciborium = "0.2.2"
dasl = "0.2.0"

[build-dependencies]
built = { version = "0.7", features = ["dependency-tree"] }
